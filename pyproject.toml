[build-system]
requires = ["uv_build >= 0.11.7, <0.12.0"]
build-backend = "uv_build"

[project]
version = "0.1.0"
name = "fake_news_simulator"
requires-python = ">= 3.12"
description = "Simulator for the spread of fake news in social networks."
readme = "README.md"
dependencies = [
    "pydantic>=2,<3",
]